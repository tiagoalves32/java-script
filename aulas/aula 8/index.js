/**
 * Tipos de Dados Primitivos
 * String, number, underfined 
 */
const nome = 'Luiz'; // string
const nome1 = "Luiz"; // string
const nome2 = `Luiz`; // string

const num1 = 10; // number
const num2 = 10.52; //number

let nomeAluno; /* undefined -> não aponta pra local algum na memória.
*/

const sobrenomeAluno = null; /* Nulo ->  não aponta pra local algum na memória.O uso de null é para explicitar a ação de que a variável não aponte pra nenhum local na memória.
*/

const aprovado = false; // Boolean = true, false (lógico)

